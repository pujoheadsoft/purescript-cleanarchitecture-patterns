module Test.Record.Usecase.DisplayUserInfoSpec where

import Prelude

import Test.Spec (describe, it)
import Test.Spec.Assertions (shouldEqual)

spec = do
  describe "" do
    it "" do
      "" `shouldEqual` ""